# Shared Types

This directory contains the documented data contracts and types shared between the frontend (TypeScript) and backend (Python Pydantic).

Any change to a core data type (e.g., `Document`, `Clause`, `ApiError`) must be reflected in both environments.

See [contracts.md](./contracts.md) for the definitive source of truth.
